import { BrowserRouter, Routes, Route } from 'react-router-dom'

export const Router = (): JSX.Element => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path='/' element='' />
      </Routes>
    </BrowserRouter>
  )
}
